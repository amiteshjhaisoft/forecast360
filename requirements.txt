# ---------- Core runtime ----------
python-dotenv
requests
httpx

# ---------- Streamlit app ----------
streamlit>=1.33
Pillow

# ---------- Data / compute ----------
numpy
pandas
pyarrow

# ---------- Visualization ----------
matplotlib
plotly

# ---------- File parsing / loaders used by RAG ----------
pypdf                      # PyPDFLoader
beautifulsoup4>=4.12       # BSHTMLLoader
lxml>=4.9                  # parser for bs4
docx2txt>=0.8              # Docx2txtLoader
openpyxl                   # .xlsx
xlrd==1.2.0                # legacy .xls (supports .xls in this version)
# If you truly need .xlsb: pyxlsb
# If you need ODS: odfpy
# unstructured is powerful but heavy (system deps); leave commented unless needed:
# unstructured[all-docs]>=0.15
python-pptx>=0.6.23        # PPTX parsing (used by unstructured too)
python-magic               # requires libmagic (on Windows use python-magic-bin)
PyYAML

# ---------- Classic ML / Stats ----------
scikit-learn
statsmodels
scipy

# ---------- LangChain / Vector ----------
langchain-core>=0.2
langchain-community>=0.2
langchain-text-splitters>=0.2
langchain-anthropic>=0.2
langgraph>=0.2
faiss-cpu>=1.8
sentence-transformers>=3.0  # pulls torch; keep if you use local HF embeddings

# ---------- Model SDKs ----------
anthropic>=0.34
langchain-openai>=0.2.0     # <-- bump to align with LC split packages
openai>=1.52.0

# ---------- Azure Blob Storage ----------
azure-storage-blob>=12.22.0
azure-identity>=1.17.1

# ---------- Optional (app models/features) ----------
# These are heavy; keep only if you use them in production:
pmdarima
prophet                    # pulls cmdstanpy; needs a compiler toolchain
tbats
xgboost
lightgbm
darts                      # pulls torch & co.
langsmith
