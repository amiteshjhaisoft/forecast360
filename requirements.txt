# ---------- Streamlit app ----------
streamlit>=1.33
Pillow

# ---------- Data / compute ----------
numpy
pandas
pyarrow               # needed for pd.read_parquet even if not imported directly
openpyxl              # .xlsx
xlrd==1.2.0           # legacy .xls support (this pin supports .xls)

# ---------- Visualization ----------
matplotlib

# ---------- Azure Blob sync (used by kb_sync_azure.py) ----------
azure-storage-blob>=12.22.0
azure-identity>=1.17.1

# ---------- Optional: enable only if you use these features ----------
# plotly                     # if you switch charts back to Plotly
# python-dotenv             # if you load env vars from a .env file
# requests
# httpx
# pypdf                     # PDF parsing
# beautifulsoup4
# lxml
# python-pptx
# docx2txt
# PyYAML
# python-magic; platform_system!="Windows"
# python-magic-bin; platform_system=="Windows"

# --- Time-series / ML (uncomment when models are wired in the UI) ---
# scikit-learn
# statsmodels
# scipy
# pmdarima
# prophet                  # heavy; needs compiler toolchain
# tbats
# xgboost
# lightgbm
# darts                    # heavy; pulls torch & lightning

# --- RAG / Vector / LLM (only if you actually use them) ---
# langchain-core>=0.2.14,<0.3
# langchain-community>=0.2.14,<0.3
# langchain-text-splitters>=0.2.0,<0.3
# langchain-anthropic>=0.1.0
# langgraph>=0.2.0
# faiss-cpu>=1.8; platform_system!="Windows"  # on Windows prefer Conda or skip
# sentence-transformers>=3.0                  # pulls torch
# torch                                      # CPU build is fine
# anthropic>=0.34
weaviate-client>=4.9.0
weaviate-agents>=0.0.8

# langsmith
